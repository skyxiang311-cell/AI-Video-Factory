# AI Video Factory 协作规范

## 项目定位

- 本项目用于制作面向中国抖音、小红书用户的知识型短视频。
- 默认成片规格为 1080×1920、9:16 竖屏，默认时长为 60～180 秒。
- 输入支持 PDF、文章、截图和文字。
- 第一版只开发 Knowledge 知识型模板。

## 内容原则

- 内容生产必须先理解原始资料，再提炼、重新组织并原创表达；禁止逐句翻译、拼接原文或只做简单改写。
- 优先保证原创性、前三秒钩子、信息密度、收藏价值和中国用户阅读习惯。
- 涉及国外价格时，必须明确标注货币单位，例如“日元”“人民币”，不能只出现数字。
- 事实、数字、时间与专有名词必须可追溯到输入资料或经过确认的研究结果；不确定信息要明确标注，不能编造。

## 视觉与技术原则

- 视频视觉不能机械复制原始杂志、PDF 或网页排版，应根据竖屏短视频重新设计视觉层级、节奏和信息卡片。
- AI 内容层和视频渲染层必须通过 `storyboard.json` 解耦；渲染层不得依赖临时提示词或未结构化的模型输出。
- Remotion 是主要视频视觉渲染引擎，负责动画、信息卡片、动态字幕和画面编排。
- FFmpeg 用于音视频预处理、格式转换、混流、质量检查和最终辅助处理。

## 任务产物

- 每个任务必须将中间产物保存到 `output/<job-id>/`，至少包含：
  - `source.json`
  - `analysis.json`
  - `script.json`
  - `storyboard.json`
  - `voice.mp3`
  - `subtitles.json`
  - `assets.json`
  - `final.mp4`
- 后续修改视频画面时，应复用已有内容、配音、字幕和素材清单，只重跑必要阶段，不得默认重新调用全部 AI 生成流程。
- `output/` 下的生成文件不得提交到 Git；仅保留目录占位文件。

## 第一阶段边界

- 在核心视频生产链稳定之前，不开发登录、付费、SaaS 后台、多平台自动发布等功能。
- 不提交任何真实 API Key、访问令牌、账号密码或其他凭证。
- 新功能必须服务于“输入资料 → 原创内容 → Storyboard JSON → 竖屏 MP4”主链路；与主链路无关的扩展延后处理。
